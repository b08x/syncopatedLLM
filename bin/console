#!/usr/bin/env ruby
# frozen_string_literal: true

bin_dir = File.expand_path(File.join(__dir__))
$LOAD_PATH.unshift(bin_dir) unless $LOAD_PATH.include?(bin_dir)

require "bundler/setup"
require "laudllm"

require "pry"
Pry.start
